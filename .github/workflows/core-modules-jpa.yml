name: framework-parent

on:
  push:
    branches: [ "main" ]
    paths:
      - 'core-modules/core-modules-feign/**'
      - '.github/workflows/core-modules-feign.yml'

jobs:
  call-workflow:
    uses: ./.github/workflows/maven-build.yml
    with:
      work-dir: core-modules-feign
    secrets: inherit
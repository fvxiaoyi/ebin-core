name: framework-parent

on:
  push:
    branches: [ "main" ]
    paths:
      - 'framework-parent/**'
      - '.github/workflows/framework-parent.yml'
      
env:
  WORK_DIR: framework-parent
  GITHUB_ACTOR: fvxiaoyi
  GITHUB_TOKEN: ${{ secrets.MAVEN_SERVER_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: |
        cd ${{ env.WORK_DIR }}
        mvn deploy
